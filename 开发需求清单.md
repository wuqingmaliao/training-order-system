# 培训订单管理系统 - 开发需求清单

## 一、项目概述

开发一套面向培训机构内部使用的订单管理系统，支持多角色权限控制，实现订单的录入、查询、导出和统计功能。系统为 Web 端，PC 浏览器访问即可，无需手机 App。

---

## 二、角色与权限

系统共三种角色：

| 角色 | 权限范围 |
|------|----------|
| 超级管理员 | 全部功能：查看/编辑/删除所有订单、管理用户账号、管理项目选项、导出数据、查看统计 |
| 普通管理员 | 只能查看所有订单，不能修改、删除、导出 |
| 员工 | 只能查看和录入自己的订单，不能修改、删除；可导出自己的订单 |

---

## 三、功能需求

### 3.1 登录与账号

- 首页为员工登录入口，管理员登录入口放在右上角小链接
- 不开放公开注册，账号由超级管理员在后台创建
- 不提供忘记密码功能（内部系统）
- 登录后可修改自己的密码
- 员工账号包含"所属团队"字段（如：销售一组）

### 3.2 订单管理（核心功能）

#### 3.2.1 订单字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 时间 | 日期时间 | 自动 | 默认当天，员工不可改，超管可改 |
| 学员姓名 | 文本 | 是 | |
| 身份证号 | 文本 | 是 | |
| 手机号 | 文本 | 是 | |
| 业务类型 | 下拉选择 | 是 | 选项：资质-挂靠、学历、培训、非培训 |
| 项目 | 下拉选择 | 是 | 选项由超管在后台维护，可增删 |
| 班次类别 | 文本 | 是 | |
| 收款金额 | 数字 | 是 | |
| 折后业绩 | 数字 | 是 | |
| 尾款金额 | 数字 | 是 | |
| 对接老师 | 文本 | 自动 | 自动填充当前登录员工姓名，员工不可改 |
| 是否签约 | 是/否 | 是 | 全员可见，仅超管可修改 |
| 是否回款 | 是/否 | 是 | 全员可见，仅超管可修改 |
| 备注 | 长文本 | 否 | |

#### 3.2.2 订单列表

- 列表按时间倒序排列，支持分页
- 列表直接显示以上全部字段（含身份证号和备注）
- 列顺序：时间 → 学员姓名 → 身份证号 → 手机号 → 业务类型 → 项目 → 班次类别 → 收款 → 折后业绩 → 对接老师 → 尾款 → 是否签约 → 是否回款 → 备注
- 尾款大于 0 时红色显示
- 支持搜索：按学员姓名、手机号、身份证号模糊搜索
- 超管额外支持按业务类型、签约状态、回款状态、所属员工筛选
- 员工只能看到自己的订单；超管和普通管理员能看到所有人的订单

#### 3.2.3 订单操作

- **员工**：可以新增订单、查看自己的订单详情；提交后不能修改、不能删除
- **超管**：可以查看、编辑、删除任意订单；编辑时可修改所有字段（包括时间、签约状态、回款状态）
- **普通管理员**：只能查看，不能编辑、删除

### 3.3 Excel 导出

- **超管**：可导出全部订单（受当前筛选条件影响），导出为 Excel
- **员工**：可导出自己的订单（受搜索条件影响），导出为 Excel
- 导出字段与列表一致

### 3.4 人员管理（仅超管）

- 查看所有用户列表（姓名、登录账号、角色、所属团队、状态、创建时间）
- 新建用户：填写姓名、登录账号、密码、角色（员工/普通管理员）、所属团队
- 启用/禁用用户账号

### 3.5 项目选项管理（仅超管）

- 维护"项目"下拉框的选项列表
- 可添加、删除选项
- 默认选项包括：二级建造师、一级建造师、消防工程师、监理工程师、造价工程师、安全工程师、建工单位、学历、三类、七大员、技工、特种工、公路水运检测师、中级经济师、执业药师、健康管理师、初中高级职称、论文

### 3.6 数据统计（仅超管）

- 订单总数、总收款金额、总尾款金额等汇总数据
- 各员工订单数量和收款金额排行

---

## 四、页面清单

| 页面 | 访问角色 | 说明 |
|------|----------|------|
| 员工登录页 | 所有人 | 首页，员工输入账号密码登录 |
| 管理员登录页 | 管理员 | 右上角链接进入 |
| 员工订单页 | 员工 | 自己的订单列表、新增订单、导出、查看详情 |
| 管理后台 | 超管/普通管理员 | 订单管理、人员管理、项目选项、数据统计（Tab 切换） |

---

## 五、非功能需求

1. **界面**：简洁美观，PC 端适配，参考主流后台管理系统风格
2. **性能**：常规分页查询，支持至少万级订单数据
3. **安全**：密码加密存储，接口需登录鉴权，按角色控制权限
4. **浏览器**：支持 Chrome、Edge 主流浏览器
5. **并发**：10～20 人同时使用

---

## 六、部署要求

- 提供完整源代码
- 协助部署上线（服务器/域名/数据库由甲方提供或协助选择方案）
- 部署后可通过域名直接访问
- 提供简单的使用说明文档

---

## 七、交付物

1. 完整前端 + 后端源代码
2. 数据库建表脚本或自动建表
3. 部署文档
4. 使用说明文档
5. 部署上线可访问的系统

---

## 八、参考技术栈（可调整）

以下为参考方案，开发者可根据自身技术栈选择：

- 前端：React / Vue + 主流 UI 组件库
- 后端：Node.js / Java / Python 均可
- 数据库：MySQL / PostgreSQL
- 部署：云服务器或 Serverless 平台均可
