# UI 设计指南

> **设计类型**: App 设计（应用架构设计）
> **确认检查**: 本指南适用于可交互的应用/网站/工具。

> ℹ️ Section 1 为设计意图与决策上下文。Code agent 实现时以 Section 2 及之后的具体参数为准。

## 1. Design Archetype (设计原型)

### 1.1 内容理解

- **目标用户**: 培训行业客户（填写端）+ 内部管理员（管理端），高频数据处理场景
- **核心目的**: 高效采集订单信息 + 清晰管理/打印/导出数据
- **情绪基调**: 专业可信 / 避免焦虑与视觉疲劳

### 1.2 设计方向

- **Design Style**: Soft Blocks 柔色块 — 表单与管理后台需兼顾亲和力与信息密度，柔和圆角降低填写压力
- **Application Type**: SaaS/Admin + Public Form 双端混合应用
- **Aesthetic Direction**: 藏青主色建立商务信任，浅灰底色承载高密度数据，白色卡片聚焦操作区

## 2. Color System (色彩系统)

**色彩关系**: 藏青主色 + 冷灰背景 + 纯白容器 + 语义状态色
**配色设计理由**: 培训行业需专业感，藏青比默认蓝更沉稳；浅灰底减少长时间办公视觉疲劳
**主色推导**: 藏青色对应「合同/签约」业务对象的严肃性，用于提交、激活等关键行动
**使用比例**: 60% 冷灰背景 / 30% 白色卡片 / 10% 藏青主色仅用于按钮、激活态、表头强调

### 2.1 主题颜色

| Token                | HSL 值                 | 说明                                   |
| -------------------- | ---------------------- | -------------------------------------- |
| `background`         | hsl(220, 20%, 97%)     | 冷灰页面底色，降低数据密集区的视觉噪音 |
| `card`               | hsl(0, 0%, 100%)       | 表单/列表/详情卡片背景                 |
| `foreground`         | hsl(222, 47%, 11%)     | 主文字，深藏青墨色                     |
| `muted-foreground`   | hsl(220, 9%, 46%)      | 次要说明、占位符、禁用文字             |
| `primary`            | hsl(215, 60%, 18%)     | 藏青主交互色，提交/登录/激活           |
| `primary-foreground` | hsl(0, 0%, 100%)       | 主按钮文字                             |
| `accent`             | hsl(215, 30%, 95%)     | Ghost 按钮 hover、下拉 focus、骨架屏   |
| `accent-foreground`  | hsl(215, 60%, 18%)     | accent 上的文字/图标                   |
| `border`             | hsl(220, 13%, 91%)     | 输入框/卡片/分隔线边框                 |

### 2.2 导航区配色

- **基调关系**: 顶部操作栏使用 `hsl(220, 25%, 14%)` 深灰底色 + 白字，与内容区形成明确层级
- **关键状态**: 激活项用 `primary-foreground` 高亮；Hover 用半透明白 `rgba(255,255,255,0.1)`
- **边界与背景**: 非透明深色背景，底部无分割线，靠色差异区分

### 2.3 语义颜色

| 用途     | Token        | HSL 值              | 衍生说明                          |
| -------- | ------------ | ------------------- | --------------------------------- |
| 成功     | `success`    | hsl(142, 71%, 45%)  | 表单提交成功提示、合同已签状态    |
| 警告     | `warning`    | hsl(38, 92%, 50%)   | 尾款未付提醒，大字号使用          |
| 错误     | `destructive`| hsl(0, 84%, 60%)    | 验证失败、登录错误                |
| 信息标签 | `info-badge` | hsl(215, 60%, 95%)  | 培训类型/报考项目等分类标签背景   |

## 3. Typography (字体排版)

- **Heading**: Inter, "PingFang SC", "Microsoft YaHei", sans-serif
- **Body**: Inter, "PingFang SC", "Microsoft YaHei", sans-serif
- **Monospace**: JetBrains Mono, "SF Mono", monospace（金额/身份证号/手机号数字对齐）
- **字体策略**: Inter 提供西文专业感与数字等宽特性；中文回退优先苹方/微软雅黑保证表单标签清晰度

## 4. Layout Strategy (布局策略)

- **导航意图**: 管理后台采用 Topbar 导航（概设已声明顶部操作栏）；表单页无全局导航，仅品牌标题
- **页面架构**: 表单页 `max-w-2xl` 居中；后台页 `max-w-[1400px]` 全宽表格；统一响应式内边距
- **响应式**: 移动端表单全宽单列、后台表格转卡片列表、详情面板全屏抽屉

## 5. Visual Language (视觉语言)

- **形态参数**: 圆角 `rounded-lg (0.5rem)` · 阴影 `shadow-sm` · 间距基调 `standard`
- **识别签名**: 表单分组用左侧 3px 藏青竖条标识；金额数字右对齐 + 等宽字体；状态标签 pill 形 + 低饱和底色
- **装饰策略**: 仅用分组竖条与状态胶囊标签，无多余装饰图形
- **动效原则**: 交互反馈 150ms ease-out；抽屉滑出 250ms ease-in-out
- **可及性**: 正文对比度 ≥ 4.5:1；深色顶栏白字对比度 ≥ 7:1；打印样式隐藏所有交互元素

## 6. Component Principles (组件原则)

- **状态完整性**: Button/Input/Select 覆盖 Default/Hover/Focus/Disabled/Error；Focus ring 用 `ring-2 ring-primary/30`
- **层级清晰**: Primary 按钮藏青填充；Secondary 按钮白底藏青边框；Ghost 按钮仅 hover 显底色
- **一致性**: 表单字段 label 在上、input 在下、间距 `space-y-4`；表格行高 `h-12` 统一；分页控件居右
- **打印专属**: A4 打印样式移除 shadow/border-radius/background-color；标题用 18pt 加粗；字段用两列网格排布

## 7. Image Direction (图片与视觉资产)

- **Image Role**: 无强制图片需求
- **Image Art Direction**: 优先通过排版、色彩和局部图形建立视觉记忆点；表单页头部可用极简线性图标辅助分组识别
- **Image Prompt Keywords**: 无
- **Image Avoidance**: 避免通用商务人物素材、科技感插图、渐变背景图

## 8. 应避免 (Anti-patterns)

- ❌ 表单页添加 Sidebar/Footer/营销区块（概设未声明，公开链接应保持极简）
- ❌ 管理后台使用紫色/亮蓝等高饱和主色（破坏培训行业商务信任感）
- ❌ 打印样式保留导航栏/按钮/阴影/圆角（A4 输出需纯净文档感）