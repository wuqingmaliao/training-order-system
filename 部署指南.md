# 培训订单管理系统 - 部署指南

## 快速部署（3步搞定）

### 第1步：安装 Docker Desktop

在目标电脑上下载并安装 Docker Desktop：
- 下载地址：https://www.docker.com/products/docker-desktop/
- 安装完成后**重启电脑**
- 启动 Docker Desktop（等待桌面右下角鲸鱼图标变成绿色，表示就绪）

### 第2步：复制项目文件夹

把整个 `app_17btu6ueem0` 文件夹复制到目标电脑上（可以用U盘、网盘、或者直接压缩发送）。

### 第3步：双击启动

双击文件夹里的 **`start.bat`**，等待自动构建和启动（首次约5-10分钟）。

看到 "✅ 系统启动成功" 就完成了！

---

## 访问系统

| 用途 | 地址 |
|------|------|
| 客户填写订单 | http://localhost:3000 |
| 管理员后台 | http://localhost:3000/admin/login |
| 默认管理员密码 | `admin123` |

---

## 给其他人用

### 同一局域网（同一个WiFi/公司网络）

1. 在运行服务的电脑上查看 IP 地址：
   - 按 `Win + R`，输入 `cmd` 回车
   - 输入 `ipconfig`，找到 **IPv4 地址**（类似 `192.168.1.100`）

2. 其他人通过这个地址访问：
   - 订单表单：`http://192.168.1.100:3000`（把IP换成你自己的）
   - 把这个链接发给同事/客户即可

3. **如果别人访问不了**，需要开放防火墙端口：
   - 打开「Windows Defender 防火墙」→「高级设置」
   - 点击「入站规则」→「新建规则」
   - 选择「端口」→ 下一步
   - 选择「TCP」，特定本地端口填 `3000` → 下一步
   - 选择「允许连接」→ 下一步 → 下一步
   - 名称填「培训订单系统」→ 完成

### 外网访问（不同网络/给客户用）

如果需要让不在同一网络的人访问，推荐用内网穿透工具：

**ngrok（最简单）**
1. 下载 ngrok：https://ngrok.com/
2. 解压后在命令行运行：`ngrok http 3000`
3. 会给你一个公网地址（类似 `https://xxxx.ngrok.io`）
4. 把这个地址发给客户即可

---

## 常用操作

### 启动服务
双击 `start.bat`

### 停止服务
双击 `stop.bat`

### 查看运行状态
打开命令行，进入项目文件夹，运行：
```
docker compose ps
```

### 查看日志
```
docker compose logs app
```

### 修改管理员密码
1. 用记事本打开 `docker-compose.yml`
2. 找到 `ADMIN_PASSWORD: admin123`
3. 改成你要的密码
4. 保存后重新双击 `start.bat`

### 修改端口（如果3000被占用）
1. 用记事本打开 `docker-compose.yml`
2. 找到 `"3000:3000"`，改成 `"8080:3000"`（用8080端口）
3. 保存后重新启动
4. 访问地址变成 http://localhost:8080

### 备份数据
```
docker compose exec postgres pg_dump -U training_user training_orders > backup.sql
```

### 恢复数据
```
docker compose exec -T postgres psql -U training_user -d training_orders < backup.sql
```

---

## 常见问题

**Q: 双击 start.bat 后闪退？**
A: 右键点击 start.bat → 以管理员身份运行。或者先打开命令行，cd 到项目目录，运行 `docker compose up` 看错误信息。

**Q: 提示 "docker 不是内部命令"？**
A: Docker Desktop 没安装或没启动。先安装 Docker Desktop 并启动它。

**Q: 启动后访问页面空白？**
A: 等待10-20秒让服务完全启动，然后刷新页面。如果还是不行，运行 `docker compose logs app` 查看错误。

**Q: 数据存在哪里？会丢失吗？**
A: 数据保存在 Docker 卷中，停止/重启服务不会丢失。只有运行 `docker compose down -v` 才会删除数据。

**Q: 怎么升级到新版本？**
A: 替换项目文件（保留 docker-compose.yml 中的密码配置），重新双击 start.bat 即可。数据库数据不会丢失。

---

## 系统要求

- Windows 10/11（64位）
- 至少 4GB 内存
- 至少 10GB 可用磁盘空间
- 网络连接（首次构建需要下载依赖）

---

## 文件说明

| 文件 | 作用 |
|------|------|
| `start.bat` | 一键启动脚本（双击运行） |
| `stop.bat` | 停止服务脚本 |
| `docker-compose.yml` | 服务配置（端口、密码等） |
| `Dockerfile` | 应用镜像构建文件 |
| `docker/init.sql` | 数据库初始化脚本 |
| `docker/entrypoint.sh` | 容器启动脚本 |
| `.dockerignore` | 构建忽略文件 |
| `部署指南.md` | 本说明文档 |
