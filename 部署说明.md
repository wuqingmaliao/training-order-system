# 筑一教育订单管理系统 - 部署说明

## 一、环境要求

- Node.js 18 或以上版本（下载地址：https://nodejs.org ，选 LTS 版本）
- npm 9 以上（随 Node.js 一起安装）
- Windows / Linux / macOS 均可

---

## 二、本地运行（用于测试）

本地运行使用 SQLite 数据库，无需额外安装数据库软件。

1. 解压源代码包
2. 打开命令行，进入项目目录：
```bash
cd 项目目录
```
3. 安装依赖：
```bash
npm install --legacy-peer-deps
```
4. 构建项目：
```bash
npm run build:server
npm run build:client
```
5. 启动服务：
```bash
set NODE_ENV=production
node dist/server/main.js
```
6. 浏览器打开：http://localhost:3000
7. 默认管理员账号：admin / admin123

> 本地数据保存在 `data/training.db` 文件中，删除该文件会清空所有数据。

---

## 三、线上部署

### 方案 A：免费部署（推荐）

使用 Vercel（托管服务）+ Supabase（数据库），均有免费额度，适合 20 人以内使用。

#### 第一步：注册账号

1. 注册 Supabase：https://supabase.com （可用 GitHub 账号登录）
2. 注册 Vercel：https://vercel.com （可用 GitHub 账号登录）
3. 注册 GitHub：https://github.com （如已有可跳过）

#### 第二步：创建数据库

1. 登录 Supabase，点击 "New Project"
2. 填写项目名称、数据库密码（请记住密码），选择离你最近的区域
3. 等待项目创建完成（约 2 分钟）
4. 进入 Project Settings → Database → Connection string
5. 选择 "Transaction mode"（端口 6543），复制连接串，格式如下：
```
postgresql://postgres.xxxx:你的密码@aws-0-xx.pooler.supabase.com:6543/postgres
```

#### 第三步：推送代码到 GitHub

1. 在 GitHub 创建一个新仓库（设为 Private）
2. 将代码推送到该仓库：
```bash
git init
git add .
git commit -m "init"
git remote add origin 你的仓库地址
git push -u origin master
```

#### 第四步：在 Vercel 部署

1. 登录 Vercel，点击 "Add New Project"
2. 选择刚才的 GitHub 仓库，点击 "Import"
3. Framework Preset 选择 "Other"
4. Environment Variables 添加：
   - Key: `DATABASE_URL`，Value: 第二步复制的 Supabase 连接串
5. 点击 "Deploy"，等待部署完成（约 2-3 分钟）
6. 部署完成后会得到一个 `xxx.vercel.app` 的访问地址

#### 第五步：绑定自己的域名（可选）

1. 在 Vercel 项目 → Settings → Domains
2. 输入你的域名，按提示在域名服务商处添加 CNAME 记录
3. 等待 DNS 生效后即可通过自己的域名访问
4. Vercel 会自动配置 HTTPS 证书

> 注意：vercel.app 域名在国内可能无法访问，建议绑定自己的域名。

---

### 方案 B：自有服务器部署

适合有自己服务器（云服务器或内网服务器）的场景。

#### 服务器要求

- 2核 4G 以上配置
- 已安装 Node.js 18+
- 已安装 PostgreSQL 14+（或使用 SQLite 免安装）
- 如使用 PostgreSQL，创建一个空数据库：
```sql
CREATE DATABASE training_order;
```

#### 部署步骤

1. 将代码上传到服务器（git clone 或解压 zip）
2. 进入项目目录，安装依赖：
```bash
npm install --legacy-peer-deps
```
3. 创建 `.env` 文件：
```
DATABASE_URL=postgresql://用户名:密码@localhost:5432/training_order
TOKEN_SECRET=请替换为一串随机字符
NODE_ENV=production
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
```
> 如果使用 SQLite，不设 DATABASE_URL 即可，数据自动存在 data/training.db。

4. 构建并启动：
```bash
npm run build:server
npm run build:client
npm install -g pm2
pm2 start dist/server/main.js --name training-order
pm2 save
pm2 startup
```

5. 安装 Nginx 反向代理（以域名 your-domain.com 为例）：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

6. 使用 certbot 申请免费 HTTPS 证书：
```bash
certbot --nginx -d your-domain.com
```

7. 访问 https://your-domain.com ，使用 admin / admin123 登录

---

## 四、环境变量说明

| 变量名 | 必填 | 说明 |
|--------|------|------|
| DATABASE_URL | 线上必填 | PostgreSQL 连接串，不填则使用本地 SQLite |
| TOKEN_SECRET | 建议设置 | 登录令牌加密密钥，建议设为一串随机字符 |
| NODE_ENV | 建议设置 | 设为 production |
| SERVER_HOST | 可选 | 监听地址，默认 0.0.0.0 |
| SERVER_PORT | 可选 | 监听端口，默认 3000 |

---

## 五、默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 超级管理员 | admin | admin123 |

> 系统首次启动时会自动创建该账号。登录后请及时修改密码。超级管理员可在"人员管理"中创建员工和普通管理员账号。

---

## 六、常用维护命令

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs training-order

# 重启服务
pm2 restart training-order

# 停止服务
pm2 stop training-order

# 更新代码后重新部署
git pull
npm install --legacy-peer-deps
npm run build:server
npm run build:client
pm2 restart training-order
```

---

## 七、技术栈

- 前端：React 19 + Vite + Tailwind CSS + shadcn/ui
- 后端：NestJS + Drizzle ORM
- 数据库：PostgreSQL（线上）/ SQLite（本地）
- 部署：Vercel + Supabase（免费方案）或自有服务器
